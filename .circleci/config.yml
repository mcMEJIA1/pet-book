version: 2

jobs:
  build:
    working_directory: ~/pet-book
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - restore_cache:
          key:
          - v1-dependencies-{{ checksum "package-lock.json" }}
      - run: 
          name: Install local dependencies
          command: npm ci
      - save_cache:
          key: v1-dependencies-{{ checksum "package-lock.json" }}
          paths:
              - node_modules
      - run: 
          name: unit-test
          command: npm run test-ci-2